package dev.ckateptb.minecraft.caffeine;

import org.bukkit.plugin.java.JavaPlugin;

public class Caffeine extends JavaPlugin {
    // The plugin includes the Caffeine library,
    // which manages all the logic. The plugin does not need
    // additional code. The plugin itself is necessary so as not
    // to duplicate the library.
}